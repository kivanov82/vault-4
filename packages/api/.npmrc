; @x402/express lists @x402/paywall as an optional peer; @x402/paywall pulls
; react@19, which npm's strict peer resolver rejects in this backend (no React).
; We don't use the paywall UI (basic JSON 402), so relax peer resolution.
legacy-peer-deps=true
